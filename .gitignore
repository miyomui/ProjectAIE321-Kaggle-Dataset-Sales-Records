# 1. ป้องกันไฟล์กุญแจความลับ (สำคัญที่สุด ห้ามลบ)
credentials.json

# 2. ป้องกันไฟล์ข้อมูลขนาดใหญ่ (Github ไม่ให้เก็บไฟล์ใหญ่)
# data/
# *.csv

# 3. ป้องกันไฟล์ขยะของระบบ (ไม่ต้องส่งไปให้เพื่อน)
venv/
__pycache__/
.DS_Store
.env